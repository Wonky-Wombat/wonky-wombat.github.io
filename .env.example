# Base URL of the Catspace web gateway. Leave unset to hide the emotion demo.
# Inlined into the static build, so it must be a public URL (never a secret).
# Local development (gateway from Catspace-Algorithm/services/cat-web-gateway):
NEXT_PUBLIC_GATEWAY_URL=http://localhost:8080
